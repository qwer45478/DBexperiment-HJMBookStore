spring:
  application:
    name: hjm-bookstore
  
  datasource:
    url: jdbc:mysql://47.101.61.134:3306/book_store_database?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: book_store_database
    password: 8XzYEiwxem7EbNGZ
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
        
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080
  servlet:
    context-path: /api

# JWT Configuration
jwt:
  secret: hjm-bookstore-secret-key-2024-very-long-and-secure-key-for-jwt-token-generation
  expiration: 86400000 # 24 hours in milliseconds

# File Upload Path
file:
  upload-dir: ./uploads

# Application Configuration
app:
  upload:
    dir: ./uploads

# Logging
logging:
  level:
    com.hjm.bookstore: DEBUG
    org.springframework.web: INFO
    org.hibernate: INFO
