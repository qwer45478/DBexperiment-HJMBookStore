# 并发控制实现文档

## 1. 问题背景
在电商系统中，"超卖"是一个典型的高并发问题。当多个用户同时购买同一件商品（尤其是库存有限的热门商品）时，如果系统采用简单的 "查询库存 -> 内存计算 -> 更新库存" 的逻辑，会导致多个线程读取到相同的初始库存，从而覆盖彼此的更新，最终导致卖出的商品数量超过实际库存。

## 2. 解决方案：数据库原子更新 (Atomic Update)

本项目采用了 **基于数据库行锁的原子更新** 方案来解决超卖问题。

### 2.1 核心原理
利用数据库（MySQL）的行级锁（Row Lock）特性。当执行 `UPDATE` 语句时，数据库会对目标行加排他锁（X锁）。在事务提交之前，其他事务无法修改该行。通过将库存扣减操作合并为一条带有条件判断的 SQL 语句，确保了操作的原子性。

### 2.2 实现细节

#### Repository 层 (`BooksInfoRepository.java`)
定义了自定义 SQL 更新方法，直接在数据库层面完成扣减和判断：

```java
@Modifying
@Query("UPDATE BooksInfo b SET b.stock = b.stock - :quantity, b.sales = b.sales + :quantity, b.monthlySales = b.monthlySales + :quantity WHERE b.bookId = :bookId AND b.stock >= :quantity")
int decreaseStock(Integer bookId, Integer quantity);
```

*   **关键点**：`AND b.stock >= :quantity`
    *   这个条件确保了只有在库存充足时，更新才会执行。
    *   如果库存不足，SQL 执行后受影响的行数（return value）为 0。

#### Service 层 (`BookService.java`)
重构了销量更新逻辑，不再依赖内存计算：

```java
@Transactional
public void updateSales(Integer bookId, Integer quantity) {
    // 直接调用原子更新方法
    int updatedRows = booksInfoRepository.decreaseStock(bookId, quantity);
    
    // 根据返回值判断是否扣减成功
    if (updatedRows == 0) {
        // 抛出异常触发事务回滚
        throw new RuntimeException("库存不足或书籍不存在");
    }
}
```

#### 事务优化 (`OrderService.java`)
调整了订单创建流程中的操作顺序，以降低死锁概率：

1.  **先扣减库存** (`bookService.updateSales`)：尽早获取热点数据（书籍库存）的行锁。
2.  **后保存订单** (`shoppingHistRepository.save`)：执行非热点数据的插入操作。

*注意：在高并发下，MySQL 仍可能检测到死锁（Deadlock）并主动回滚部分事务，这是正常的保护机制，表现为部分用户抢购失败。*

## 3. 验证结果
通过 Python 脚本模拟 20 个并发线程抢购库存为 5 的书籍：
*   **预期**：成功 5 次，失败 15 次，最终库存 0。
*   **实测**：成功 5 次，失败 15 次，最终库存 0。

测试证明该方案有效防止了超卖现象，保证了数据的一致性。
